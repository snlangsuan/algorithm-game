# HANDOFF — read this fully before any work

Generated: 2026-09-20T16:20Z · Git HEAD: `b226e2c` (งานเกือบทั้งหมดยัง **ไม่ commit** — ดู §2)
To resume: run /handoff — it re-verifies this file against the current repo before proceeding.
Trust rule: `[V]` = ตรวจด้วยคำสั่งจริงในรอบเขียนไฟล์นี้ · `[?]` = จำมา/ยกมาจากฉบับก่อน ให้ตรวจซ้ำก่อนเชื่อ

## 1. Current goal

เกมสอนอัลกอริทึมภาษาไทย (Nuxt SPA, `ssr: false`, ไม่มี backend) — เด็กเขียนสมองให้บอทด้วยการ**ต่อบล็อก**
[V] ตอนนี้มี **5 เกม** (`app/data/games.ts`): `/maze` `/othello` `/chase` `/dino` `/hanoi`
[V] และหน้าความรู้ **23 หัวข้อ** (`app/data/algorithms.ts`)
งานรอบล่าสุด: เพิ่มเกมที่ห้า **"วิ่งหลบไม่รู้จบ" `/dino`** — เล่นเองก็ได้ ให้บอทเล่นก็ได้ · เสร็จแล้ว ไม่มีงานค้างกลางคัน

## 2. Verified state — where the work ACTUALLY stands

- [V] `npm test` ผ่าน **637/637** (ผลรันในรอบนี้ ดูท้ายหัวข้อ)
- [V] `npm run build` ผ่าน · 1.86 MB (456 kB gzip)
- [V] `npx nuxt prepare` ไม่มีคำเตือน "Duplicated imports" แล้วสักรายการ (เคยมี 18 บรรทัดต่อการบูตหนึ่งครั้ง)
- [V] **git มี 1 commit เดียว (`b226e2c`) ที่เหลือยังไม่ commit** — 82 ไฟล์แก้ + ไฟล์ใหม่ที่ยังไม่ track
  (`app/game/{chase,hanoi,dino}/`, `app/components/{chase,hanoi,dino}/`, `app/composables/use{Chase,Hanoi,Dino}Game.ts`,
  `app/data/games.ts`, `app/pages/{chase,hanoi,dino}.vue`, `app/assets/dino/`, `test/{chase,chase-pilot,hanoi,dino,othello-training}.test.ts`)
  **งานสามเกมหลัง (chase/hanoi/dino) จะหายทั้งหมดถ้าใครเผลอ `git checkout .`**
- [V] เกม `/dino` เล่นได้จริงทั้งสองโหมด ตรวจด้วย Chrome จริงผ่าน CDP ในรอบนี้:
  บอทวิ่งเอง (ระดับ 1→3, ความเร็ว 337→462 px/วิ) · คนเล่นเอง (เว้นวรรคกระโดด 3 ครั้ง, ลูกศรลงหมอบ 1 ครั้ง)
- [V] ขนาดไฟล์แกนของเกมใหม่: `game/dino/engine.ts` 444 · `blocks/pack.ts` 413 · `agent.ts` 193 ·
  `agent.worker.ts` 259 · `art.ts` 115 · `runner.ts` 205 · `useDinoGame.ts` 598 · `DinoTrack.vue` 222 · `test/dino.test.ts` 212
- [V] ฟิสิกส์ของเกมใหม่: `STEP = 1/60` วินาที · `DECIDE_EVERY = 2` (ถามสมอง 30 ครั้ง/วินาที) ·
  `GRAVITY = 1150` · `JUMP_SPEED = 350` → ลอย **0.609 วิ** สูงสุด **53.3 px** · `SIGHT = 562 px` · `LEVEL_SPAN = 1200 px`
- [V] กล่องชน (`app/game/dino/art.ts`): ยืน 15×20 · หมอบ 19×11 · ฝูงนก 24×52 ลอยสูงจากพื้น 13
- [V] ตัวเลขที่หน้าความรู้อ้าง ยังตรงกับเอนจินจริง (รันสคริปต์กวาดค่าในรอบนี้ ลู่คลาสสิก 8 เมล็ด):
  เผื่อเป็นเวลา 0.15–0.30 วิ → เฉลี่ย **12,270 ม.** · 0.10 วิ → 211 ม. · 0.40 วิ → 141 ม. ·
  ระยะตายตัวดีที่สุด 90 px → **1,299 ม.** (70 px → 960 ม.)
- [?] เกมเก่าทั้งสี่ (maze/othello/chase/hanoi) ไม่ได้แตะรอบนี้นอกจากย้าย `SPEEDS` ออกจาก composable —
  เปิดทั้งห้าหน้าแล้วไม่มี error ในคอนโซล แต่ไม่ได้เล่นจนจบเกม

### Test output (tail, from this handoff run)

```
1..637
# tests 637
# suites 0
# pass 637
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 6977.982209
```

## 3. Decisions made & WHY

### เกมวิ่งหลบ `/dino`

- **ฟิสิกส์ต่อเนื่อง (y/vy + แรงโน้มถ่วง) ไม่ใช่เดินทีละช่อง** — รุ่นแรกทำเป็นช่อง/จังหวะแล้วผู้ใช้ปฏิเสธ (ดู §4 ข้อ 1)
  หน่วยทั้งเกมเป็น **พิกเซลกับวินาที** เหมือนที่คนทำเกมใช้จริง [V] `engine.ts`
- **เวลาเดินก้าวคงที่ + ถามสมองทุก 2 เฟรม** เพื่อให้เมล็ดสุ่มเดิมได้ผลเดิมเป๊ะ — เทสต์และตัวเลขในหน้าความรู้พึ่งข้อนี้
  ปุ่ม "ความเร็วภาพ" จึงเปลี่ยนแค่เวลานาฬิกาจริง (`scale` ใน `game/dino/pace.ts`) ไม่แตะฟิสิกส์ [V]
- **ไม่มีเส้นชัย + ความเร็วไต่ขึ้นไม่มีเพดาน** (ผู้ใช้สั่ง "เป็น infinity นะ") — สายตาคงที่ 562 px
  พอเร็วขึ้นเรื่อย ๆ เวลาที่เห็นล่วงหน้าจะสั้นกว่าที่กฎต้องใช้ ทุกกฎจึงแพ้ในที่สุด **ระยะทางจึงเป็นคะแนนวัดอัลกอริทึม** [V]
- **นกมาเป็นฝูงสองตัว** ไม่ใช่ตัวเดียว (ดู §4 ข้อ 4) [V] `art.ts` `stack: 2`
- **ภาพใช้ asset ที่ผู้ใช้ส่งมา** (`/Users/decimo/Downloads/assets` → `app/assets/dino/`) เป็นชุดของคลิปสอน Godot
  ตัวละคร mort 24 เฟรม · นก 9 เฟรม · ถัง/ตอไม้/ก้อนหิน · พื้น · ฉากหลัง 5 ชั้น [V] ไฟล์ครบใน `app/assets/dino/`
- **ฉากหลัง+พื้นวาดด้วย CSS `background-position` ส่วนตัวละคร/สิ่งกีดขวางวาดใน SVG** ที่ใช้พิกัดเดียวกับกติกา
  ฉากที่ต่อไม่รู้จบเลื่อนด้วย CSS ถูกกว่ามาก ส่วนของที่ต้องตรงกล่องชนต้องอยู่ระบบพิกัดเดียวกับเอนจิน [V] `DinoTrack.vue`
- **เฟรมวิ่ง/ปีกนกสลับตามเวลาจริง ไม่ผูกกับเฟรมฟิสิกส์** — ตั้งความเร็วภาพช้าแล้วขาจะขยับช้าจนดูเหมือนภาพค้าง [V]
- **ตัวอย่างสามชุด**: `starter` (ระยะตายตัว 70 px ไม่ดูชนิด) · `reflex` (ความเร็ว × 0.3 วิ) · `countdown` (เวลา ≤ 0.3 วิ)
  สองตัวหลังคือกฎเดียวกันคนละหน่วย จงใจให้เทียบกันได้ [V] `blocks/pack.ts`

### ทั้งโปรเจกต์

- [V] **`SPEEDS`/`SpeedOption` ต้องอยู่นอก `app/composables/`** — ย้ายไป `app/game/<เกม>/pace.ts` ทั้งห้าเกมแล้ว
  และ `RunResult` เปลี่ยนเป็น `MazeRunResult` / `HanoiRunResult` (ดู §5)
- [?] **บล็อกเป็นทางเดียว** โหมดเขียนโค้ด JS ยังอยู่ในไฟล์แต่กดเข้าไม่ถึง (~1,900 บรรทัด: `MazeCodeEditor.vue`,
  `OthelloCodeEditor.vue`, `maze/templates.ts`, `othello/templates.ts`) **ยังไม่ตัดสินใจว่าจะลบ — ห้ามลบเองโดยไม่ถาม**
  `importer.ts` ห้ามลบเด็ดขาด เทสต์ roundtrip ใช้อยู่
- [?] **หนึ่งบล็อก = หนึ่งบรรทัด** กติกาตายตัว ทำให้ map บรรทัด↔บล็อก 1:1 และไฮไลต์ตอนรันได้
- [?] **ตัวอย่างสำเร็จรูปแก้ไม่ได้** ต้องกด "คัดลอกไปแก้" ก่อน (`locked` + `cloneForEditing` ใน `useBlockProgram`)
- [?] **ภาพในหน้าความรู้คำนวณสดจากเอนจินจริง** (`app/data/algorithm-figures.ts`) ไม่ใช่รูปแคป เพราะรูปแคปจะโกหกเงียบ ๆ

## 4. Failed approaches / dead ends — DO NOT RETRY THESE

1. **[V] ทำเกมวิ่งเป็นกริด (เดินทีละช่อง ทีละจังหวะ)** → ความสูงมีแค่ 3 ระดับ การกระโดดเลยกระตุกเป็นขั้นบันได
   ผู้ใช้บอกตรง ๆ ว่า "movement แปลกมาก ก็กระโดดที่ slow แปลกๆ" · **ห้ามกลับไปทำเป็นช่อง** ต้องเป็น float + แรงโน้มถ่วง
2. **[V] วาดตัวละครเองด้วย SVG** ลองสองรอบ — ทรงบล็อกแบบไดโน Chrome แล้วก็แบบการ์ตูนมีสี
   ผู้ใช้ปฏิเสธทั้งคู่ ("dino พันธ์อะไรเนี่ย รูปกากมากๆๆ") · จบที่ใช้สไปรท์ที่ผู้ใช้ส่งมา **อย่าเสียเวลาวาดเองอีก**
3. **[V] ใช้สไปรท์ไดโนของ Chrome (github loparcog/chrome-dinosaur)** → ถูกแทนด้วย asset ที่ผู้ใช้ให้มา
   ไฟล์ `app/assets/dino-sprite.png` กับ `app/game/dino/sprite.ts` ถูกลบแล้ว อย่ารื้อกลับมา
4. **[V] ให้นกเป็นตัวเดียวเป็นอุปสรรคที่ต้องหมอบ** → กระโดดสูง 53.3 px ข้ามนกที่สูงถึง 29 px ได้สบาย
   แปลว่าบล็อก "หมอบ" ไม่มีความหมายเลย · แก้เป็นฝูงสองตัว (กล่องชนสูง 52 ฐาน 13) กระโดดไม่พ้นแต่หมอบลอดได้
   มีเทสต์ล็อกความสัมพันธ์นี้ไว้ที่ `test/dino.test.ts` ("ฝูงนกกระโดดข้ามไม่พ้น แต่หมอบลอดได้")
5. **[V] ใส่เพดานความเร็ว (`maxSpeed`) ให้ลู่** → กฎที่คิดถูกวิ่งได้ไม่จบ ทุกตัวอย่างได้คะแนนเท่ากันหมด
   ไม่มีอะไรให้เทียบว่าอัลกอริทึมไหนดีกว่า · ตัดเพดานออกแล้ว ห้ามใส่กลับ
6. **[V] มีเส้นชัย/`goal`** → ผู้ใช้สั่งให้เป็น endless ("วิ่งเรื่อยๆ จนชน") ตอนนี้ `Outcome` เหลือ `'crashed'` อย่างเดียว
7. **[?] (ยกจากฉบับก่อน) สรุปว่า "ทำไม่ได้" โดยไม่ไล่ดู API ก่อน** — เคยเขียนว่า BFS/A* ต่อด้วยบล็อกไม่ได้
   พอไล่ดู `MazeAgent` จริงพบว่า `heap()`/`rebuild()`/`manhattan()`/`neighbors()` มีครบ ขาดแค่คำศัพท์บล็อก
8. **[?] (ยกมา) เพิ่มหัวบล็อกที่สองแล้วปล่อยให้ codegen เขียนเมธอดเปล่า** → worker เลือกโหมดจากการ override
   ต้องใส่ `skipWhenEmpty` (`blocks/pack.ts`, ใช้ที่ `codegen.ts`) เฉพาะเมธอดที่ไม่บังคับ
9. **[?] (ยกมา) ประกาศ `const` ที่ตาราง parser ใช้ ไว้ใต้ตาราง** → พังตอนโหลดโมดูล ฟ้องแค่
   `Cannot read properties of undefined (reading 'map')` · ตารางพวกนี้ต้องอยู่ใต้ของที่มันอ้างเสมอ
10. **[?] (ยกมา) ให้ BFS กับ Dijkstra ใช้บล็อกลำดับตัวเดียวกัน** → ได้เลขเท่ากันเป๊ะ = อัลกอริทึมเดียวกันคนละชื่อ
11. **[?] (ยกมา) GA ใช้แพ้/ชนะเป็น fitness** → แย่ลงชัดเจน (53%→20%) สัญญาณ 1 บิตต่อเกมหยาบเกินไป
12. **[?] (ยกมา) ทดสอบ drag ด้วย `new DragEvent()`** → ผ่านตลอดทั้งที่ลากจริงไม่ได้
    ต้องใช้ CDP `Input.setInterceptDrags` + `Input.dispatchDragEvent`
13. **[?] (ยกมา) UI ของตัวต่อบล็อก** — ไฮไลต์บล็อกที่รันต้องขาวซ้อนเงาเข้ม (`ACTIVE_RING` ใน `BlockPiece.vue`)
    ไม่ใช่สีเดียว · ช่องรับการหย่อนต้องเป็นทั้งบล็อก (ครึ่งบน=แทรกหน้า) ไม่ใช่แถบบาง · บล็อกต้อง `w-fit` ไม่ใช่ `w-full`
    · บล็อกใหม่ที่ต่อท้ายหมวดจะตกใต้ขอบจอ (กล่องเครื่องมือสูง ~520px) ผู้ใช้หาไม่เจอ

## 5. Known traps in this codebase

- [V] **Nuxt auto-import ทุก export ในโฟลเดอร์ `app/composables/`** ชื่อกลาง ๆ จึงชนกันข้ามเกมและขึ้น warning ทุกครั้งที่บูต
  ตอนนี้ `SPEEDS`/`SpeedOption` อยู่ที่ `app/game/<เกม>/pace.ts` ทั้งห้าเกม และชนิดผลลัพธ์ตั้งชื่อแยกเป็น
  `MazeRunResult`/`HanoiRunResult` · **อย่าย้ายกลับเข้า composables และอย่าตั้งชื่อ export ซ้ำกับเกมอื่น**
- [V] **`app/game/dino/art.ts` คือสัญญาระหว่างภาพกับกติกา** ทุกกล่องชนอยู่ไฟล์เดียวนี้
  คอมโพเนนต์ต้องไม่วาดล้ำกล่อง (ยกเว้นหางที่อยู่ข้างหลัง) ไม่งั้นภาพจะโกหกว่าชน/ไม่ชน
- [V] **ไฟล์ที่เทสต์ import ห้าม import .png** — เทสต์ bundle ด้วย esbuild ซึ่งไม่รู้จักไฟล์ภาพ
  ตัวเลขของสไปรท์จึงอยู่ใน `art.ts` (ไม่มี import รูป) ส่วนรูปจริง import ในไฟล์ `.vue` เท่านั้น
- [V] **ความเร็วไม่มีเพดาน จึงต้องซอยเฟรมย่อย** `MAX_SUB_STEP = 8` px ใน `engine.ts` ไม่งั้นที่ความเร็วสูง
  หนึ่งเฟรมกินหลายสิบพิกเซล แล้วถังกว้าง 22 px จะทะลุตัวไปโดยไม่ชน (มีเทสต์ "ของแคบที่สุดไม่ทะลุตัว")
- [V] **ความเป็นดีเทอร์มินิสติกของเกมวิ่งหลบ** มาจาก `STEP` + `DECIDE_EVERY` คงที่ ไม่ใช่จากนาฬิกาจริง
  ถ้าเปลี่ยนไปเดินฟิสิกส์ตามเวลาที่ผ่านจริง (delta time) เทสต์กับตัวเลขในหน้าความรู้จะพังทันที
- [?] (ยกมา) **ไฟล์คอมโพเนนต์ใหม่/Tailwind arbitrary value ใหม่ ต้องรีสตาร์ท dev server** HMR ไม่เห็น
  รอบนี้ dev server ของผู้ใช้ค้างอยู่ (พอร์ต 3000, PID 41020) และยังโชว์ warning เก่าจนกว่าจะรีสตาร์ท
- [?] (ยกมา) **`test/nuxt-shim.ts` ถูก inject ตอน bundle เทสต์** ถ้าเทสต์ฟ้อง `ref is not defined` ให้เติมชื่อในไฟล์นั้น
- [?] (ยกมา) **ภาพในหน้าความรู้ห้ามใช้ `Math.random`** ต้องรับ seed เสมอ มีเทสต์เช็คว่าสร้างสองครั้งได้ผลเท่ากัน
- [?] (ยกมา) **`preset.name` ≠ `preset.build().name`** อย่าเอามาเทียบกัน
- [?] (ยกมา) **มีการแก้ไฟล์จากนอกเซสชัน** อย่าเชื่อจำนวนเทสต์หรือเนื้อหาจากความจำ ให้รัน `npm test` ก่อนเสมอ

## 6. Next steps (ordered)

1. **ถามผู้ใช้ว่าจะ commit ไหม** — งานสามเกม (chase/hanoi/dino) ยังไม่เข้า git เลย (§2) ถ้าตกลงค่อยแยก commit เป็นเกม ๆ
2. **เสียงกับฟอนต์ที่ยังไม่ได้ใช้** — ในชุด asset ต้นทางมี `sound/jump.wav` กับ `fonts/retro.ttf`
   (ยังอยู่ที่ `/Users/decimo/Downloads/assets` ไม่ได้ copy เข้าโปรเจกต์) ถ้าจะใส่เสียง ต้องมีปุ่มปิดเสียงด้วย
3. **ตัวอย่างที่มองล่วงหน้าสองชิ้น** — บล็อก "ชิ้นถัดไปอีกชิ้น" (`rank: '2'`) มีแล้วแต่ยังไม่มีตัวอย่างไหนใช้
   กฎที่ดูสองชิ้นน่าจะไปได้ไกลกว่า เพราะจะไม่กระโดดลงไปเจอชิ้นถัดไปพอดี — ถ้าเพิ่มตัวอย่าง ต้องเพิ่มหัวข้อความรู้
   หรือใส่ใน `NOT_ALGORITHMS` ของ `test/topics-sync.test.ts` ไม่งั้นเทสต์ฟ้อง
4. **ตัดสินใจเรื่องโค้ดโหมด JS ที่เข้าไม่ถึง ~1,900 บรรทัด** (§3) — ค้างมาสามรอบแล้ว ต้องถามก่อนลบ

## 7. Open questions for the user

- **สิทธิ์ของภาพในเกมวิ่งหลบ** — asset ชุดนี้มาจากคลิปสอน Godot ที่ผู้ใช้ส่งมาเอง ถ้าจะ deploy ขึ้น GitHub Pages
  ควรเช็กสัญญาอนุญาตก่อน ยังไม่ได้ถาม
- **จะ commit งานทั้งหมดไหม และอยากได้ข้อความ commit แบบไหน** — ยังไม่เคยขออนุญาต commit ในเซสชันนี้
- **โค้ดโหมดเขียน JS ที่เข้าไม่ถึงจะเก็บหรือตัด** — ถามมาสามรอบแล้วยังไม่ได้คำตอบ อย่าลบเองเด็ดขาด
