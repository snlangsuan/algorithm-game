# HANDOFF — read this fully before any work

Generated: 2026-09-20 · Git: ไม่มี (โปรเจกต์ยังไม่ได้ init git — ตรวจการเปลี่ยนแปลงด้วยการอ่านไฟล์เท่านั้น)
To resume: run /handoff — it re-verifies this file against the current repo before proceeding.
Trust rule: `[V]` = ตรวจซ้ำด้วยคำสั่งจริงในรอบเขียนไฟล์นี้ · `[?]` = จำมา ยังไม่ได้ตรวจ

## 1. Current goal

เกมสอนอัลกอริทึมภาษาไทย (SPA, ไม่มี backend) สองเกม: เขาวงกต `/maze` กับ Othello `/othello`
เด็กเขียนโปรแกรมให้บอท**ด้วยการต่อบล็อกลากวางอย่างเดียว** — โหมดเขียนโค้ด JS ถูกถอดออกจาก UI แล้ว
มีหน้าความรู้ `/learn` 11 หัวข้อที่อ้างทฤษฎีจริงและผูกกับของในเกม ไม่มีงานค้างกลางคัน

## 2. Verified state

- [V] `npm test` ผ่าน **143/143** — roundtrip 85 · knowledge 38 · figures 9 · preset-lock 9 · learning 2
- [V] `npm run build` ผ่าน · 1.84 MB (454 kB gzip) · `ssr: false` ที่ nuxt.config.ts:10
- [V] ไล่ลิงก์ทั้งเว็บ **17 หน้า ทุกหน้ามีเนื้อหา ไม่มีลิงก์ตาย**
  (`/` `/play` `/about` `/learn` + `/learn/<11 หัวข้อ>` `/maze` `/othello`)
- [V] maze **53 บล็อก 7 ตัวอย่าง 2 หัวบล็อก** — `maze.on-turn` (เดินทีละก้าว) · `maze.on-plan` (วางแผนทั้งเส้น)
- [V] othello **39 บล็อก 5 ตัวอย่าง 3 หัวบล็อก**
- [V] หมวดบล็อก (ระบบกำหนด เกมตั้งเองไม่ได้): สั่งให้ทำ / สิ่งที่มองเห็น / เงื่อนไข-ทำซ้ำ / ตัวเลข-ข้อความ-ตัวแปร
- [V] **BFS / Dijkstra / A\* ต่อด้วยบล็อกแล้วรันได้จริง** — ทั้งสามได้ 134 ก้าว "ดีที่สุดเท่าที่เป็นไปได้"
  สำรวจ **209 / 207 / 189** ช่องตามลำดับ (A* น้อยกว่าจริงตามทฤษฎี)
- [V] เลาะกำแพงขวา 238 ก้าว · หนูหาทาง 238 ก้าว — โหมดเดินทีละก้าวไม่ถูกหัวบล็อกใหม่กระทบ
- [V] GA เรียนรู้จริง 27.8% → 35.7% (seed ตายตัว 40 รอบ ได้เลขเดิมทุกครั้ง)
- [V] ขนาดไฟล์แกน: blocks/ types(174) core(631) codegen(106) importer(463) pack(127) program(220)
  maze/blocks/pack(906) · othello/blocks/pack(528) · maze/agent(196) · useBlockProgram(175)

## 3. Decisions made & WHY

- **บล็อกเป็นทางเดียว** โหมดเขียนโค้ด JS ถอดจากหน้าเกมทั้งหมดตามที่ผู้ใช้สั่ง
- **โค้ดโหมดโค้ดยังอยู่แต่เข้าไม่ถึง ~1,900 บรรทัด** — `MazeCodeEditor.vue`, `OthelloCodeEditor.vue`,
  `maze/templates.ts`, `othello/templates.ts`, `setAuthor`, `useTemplate` ยัง build ผ่านแต่ไม่มีทางกด
  **ยังไม่ตัดสินใจว่าจะลบ — ห้ามลบเองโดยไม่ถาม** · `importer.ts` ห้ามลบเด็ดขาด เทสต์ roundtrip ใช้อยู่
- **บล็อกวางแผนเส้นทาง 11 ตัวเป็นแบบเฉพาะทาง ไม่ใช่บล็อกคิว/แมปทั่วไป** — ของยาก (`heap()` ที่
  maze/agent.ts:170, `rebuild()` ที่ 179, `manhattan()`, `neighbors()`) มีใน `MazeAgent` อยู่แล้ว
  บล็อกแค่เปิดทางไปเรียก เหมือนที่ `เป็นกำแพง` เรียก `blocked()` [V]
- **BFS/Dijkstra/A\* ต่างกันที่ค่าในช่อง "ด้วยลำดับ" ช่องเดียว** (`planWith` ที่ maze/blocks/pack.ts:511)
  BFS=จำนวนก้าว · Dijkstra=ราคาที่จ่ายมา · A*=ราคา+ระยะตรงถึงทางออก — นี่คือแก่นที่อยากให้เด็กเห็น
- **`planPush` ตัดสินจาก "ลำดับ" ที่ผู้เล่นใส่มา ไม่ใช่ราคา** (maze/blocks/pack.ts:417) จึงใช้โครงเดียวกัน
  ได้ทั้งสามอัลกอริทึม และถ้าเด็กลืมเช็ค "เคยเจอแล้ว" ก็ยังวนไม่จบ
- **ตัวอย่างสำเร็จรูปแก้ไม่ได้** ต้องกด "คัดลอกไปแก้" ก่อน (`locked` + `cloneForEditing` ใน useBlockProgram)
- **บล็อก "โค้ดของฉัน"/"ค่าจากโค้ด" ซ่อนจากกล่อง** (`hidden: true`) ผู้ใช้ยืนยัน "ไม่จำเป็นแล้ว"
  ยังลงทะเบียนอยู่ ให้ importer สร้างได้ และหน้าความรู้ใช้โชว์ส่วนที่บล็อกยังทำไม่ได้
- **`ต่อข้อความ` แปลงเป็น `` `${a}${b}` `` ไม่ใช่ `a + b`** ไม่งั้นอ่านกลับแล้วแยกไม่ออกจากบล็อก `คำนวณ`
- **หนึ่งบล็อก = หนึ่งบรรทัด** กติกาตายตัว ทำให้ map บรรทัด↔บล็อก 1:1 และไฮไลต์ตอนรันได้
- **หน้า `/learn` แบ่งกลุ่มตามความจริง** ต่อได้เลย(9) / ระบบใช้อยู่เบื้องหลัง(1=DFS) / ยังต่อไม่ได้(1=minimax)
- **ภาพในหน้าความรู้คำนวณสดจากเอนจินจริง** (`app/data/algorithm-figures.ts` 482 บรรทัด) ไม่ใช่รูปแคป
  เพราะรูปแคปจะโกหกเงียบ ๆ เมื่อแผนที่หรือกติกาเปลี่ยน · `test/figures.test.ts` ล็อกตัวเลขในคำบรรยายไว้

## 4. Failed approaches / dead ends — DO NOT RETRY

1. **สรุปว่า "ทำไม่ได้" โดยไม่ไล่ดู API ก่อน** — เคยเขียนลง `/about` ว่า BFS/A* ต่อด้วยบล็อกไม่ได้
   เพราะต้องมีบล็อกคิว/แมป พอถูกถามว่าทำไมไม่ทำให้รันได้ ไล่ดู `MazeAgent` แล้วพบว่า `heap()`,
   `rebuild()`, `manhattan()`, `neighbors()` กับโหมด `solve()` มีครบแล้ว ขาดแค่คำศัพท์บล็อก
2. **เพิ่มหัวบล็อกที่สองแล้วปล่อยให้ codegen เขียนเมธอดเปล่า** → worker ดูว่า override `solve()` ไหม
   เพื่อเลือกโหมด เขียน `solve()` เปล่าทิ้งไว้ หุ่นเดินทีละก้าวจะถูกดันเข้าโหมดวางแผนทันที
   แก้ด้วย `skipWhenEmpty` (blocks/pack.ts:57, ใช้ที่ codegen.ts:81) ใส่เฉพาะ `solve` — `step` ต้องเขียนเสมอ
3. **ประกาศ `const` ที่ `STATEMENT_PARSERS` ใช้ ไว้ใต้มัน** → พังตอนโหลดโมดูล ฟ้องแค่
   `Cannot read properties of undefined (reading 'map')` ตารางพวกนี้ต้องอยู่เหนือที่ใช้เสมอ
4. **ให้ BFS กับ Dijkstra ใช้บล็อกลำดับตัวเดียวกัน** → ได้เลขเท่ากันเป๊ะ = เป็นอัลกอริทึมเดียวกันคนละชื่อ
   ต้องมีบล็อก `จำนวนก้าวถึงเพื่อนบ้าน` แยกจาก `ราคาที่จ่ายมาถึงเพื่อนบ้าน` ถึงจะต่างกันจริง
5. **GA ใช้แพ้/ชนะเป็น fitness** → แย่ลงชัดเจน (53%→20%) สัญญาณ 1 บิตต่อเกมหยาบเกินไป
   ใช้ `ถ้า คะแนนของฉัน > ค่าที่จำไว้ → จำทั้งน้ำหนักและคะแนน`
6. **ทดสอบ drag ด้วย `new DragEvent()`** → ผ่านตลอดทั้งที่ผู้ใช้ลากจริงไม่ได้
   ต้องใช้ CDP `Input.setInterceptDrags` + `Input.dispatchDragEvent` เท่านั้น
7. **เช็ค computed style จาก element ที่มี `data-active`** → ผิดตัว คลาสไฮไลต์อยู่ที่ `firstElementChild`
   เสียเวลาไล่หาว่า "สไตล์ไม่ติด" ทั้งที่ติดตั้งแต่แรก
8. **ไฮไลต์บล็อกที่กำลังรันด้วยสีเดียว** → บล็อกมีทั้งม่วง/เหลือง/ฟ้า/เขียว `outline-ink` จมกับขอบ
   `outline-amber-300` ก็จมกับบล็อกเงื่อนไขสีอำพัน ต้องขาวซ้อนเงาเข้ม (`ACTIVE_RING` ใน BlockPiece.vue)
9. **ใส่บล็อกใหม่แล้วปล่อยไว้ท้ายหมวด** → ตกใต้ขอบจอ ผู้ใช้หาไม่เจอ ("ไม่เจอ block string")
   กรอบกล่องเครื่องมือสูง ~520px ของที่อยู่เกิน ~500px ต้องเลื่อนถึงเห็น
10. **ช่องรับการหย่อนเป็นแถบบางระหว่างบล็อก** → รู้สึกไม่ดูด ตอนนี้ทั้งบล็อกรับ
    ครึ่งบน=แทรกหน้า ครึ่งล่าง=แทรกหลัง (`onItemOver` ใน BlockList.vue)
11. **บล็อกยืดเต็มความกว้าง (`w-full`)** → ภาษารูปทรงหายหมด ผู้ใช้บอก "งงมาก" ต้องเป็น `w-fit max-w-full`
12. **ตั้งชื่อฟังก์ชันท้องถิ่นว่า `instrument` ใน worker ที่ import `instrument`** → worker โหลดไม่ขึ้น
    ฟ้องแค่ "โค้ดมีข้อผิดพลาด" หาสาเหตุยากมาก othello worker ใช้ชื่อ `watch` แทน
13. **วางบล็อกค่า (ข้อความ/ตัวเลข) ลงตำแหน่งคำสั่ง** → ไม่ลง ซึ่งถูกต้อง ต้องเสียบในรูเท่านั้น
    อย่าสรุปว่าลากพัง

## 5. Known traps

- [V] **ไฟล์คอมโพเนนต์ใหม่ต้องรีสตาร์ท dev server** Nuxt ไม่เห็นจนกว่าจะรีสตาร์ท เจอซ้ำหลายครั้ง
- [V] **Tailwind arbitrary value ใหม่ก็ต้องรีสตาร์ท** เช่น `shadow-[0_0_0_6px_rgba(...)]` HMR ไม่สร้าง utility ให้
- [?] **ห้ามตรวจ "โปรแกรมนี้เรียนรู้ได้ไหม" ด้วยการ scan โค้ดที่แปลงแล้ว** `CORE_HELPERS` มี `saveMemory(`
  อยู่ทุกโปรแกรม ใช้ `usesBlock(program, ['remember','forget'])` แทน
- [?] **maze ไม่มีความจำข้ามเกม** บล็อกจำไว้/ลืม โผล่เฉพาะ pack ที่ตั้ง `target.memory: true` (มีแต่ othello)
- [?] **ค่าเริ่มต้นของจังหวะต้องเป็นค่าใดค่าหนึ่งใน `SPEEDS`** ไม่งั้นแถบเลือกจังหวะไม่มีปุ่มไหนสว่าง
  (เคยพังที่ othello ตั้ง 400 แต่ปุ่มมี 0/200/500/1000) ตอนนี้ทั้งสองเกมอ่าน `SPEEDS` จากที่เดียว
- [?] **`preset.name` (ชื่อในดรอปดาวน์) ≠ `preset.build().name` (ชื่อโปรแกรม)** อย่าเอามาเทียบกัน
- [V] **`test/nuxt-shim.ts` ถูก inject ตอน bundle เทสต์** เพราะ `ref`/`reactive` เป็น auto-import ของ Nuxt
  ถ้าเทสต์ฟ้อง `ref is not defined` แปลว่ามีชื่อใหม่ต้องเติมในไฟล์นั้น
- [?] **จอต่ำกว่า xl (1280px) ซ่อนคอลัมน์โปรแกรม** ตัวสำรองอยู่ใน `#panel-extra` ของทั้งสองหน้าเกม
- [V] **ภาพในหน้าความรู้ห้ามใช้ `Math.random`** ต้องใช้ `createRng(seed)` เท่านั้น
  มีเทสต์เช็คว่าสร้างสองครั้งได้ผลเหมือนกัน หลุดเมื่อไรเทสต์ฟ้องทันที
- [V] **มีการแก้ไฟล์จากนอกเซสชัน** รอบที่แล้วเจอ `algorithms.ts`, `knowledge.test.ts` และไฟล์ชุด
  `algorithm-figures.ts` + `LearnFigure.vue` + `figures.test.ts` ถูกเพิ่ม/แก้ระหว่างทาง
  **อย่าเชื่อจำนวนเทสต์หรือเนื้อหาจากความจำ ให้รัน `npm test` ดูก่อนเสมอ**

## 6. Next steps (ordered)

ไม่มีงานค้างกลางคัน ข้อถัดไปคือสิ่งที่คุยไว้แต่ยังไม่ได้ทำ:

1. **ตัดสินใจเรื่องโค้ดที่เข้าไม่ถึง ~1,900 บรรทัด** (ดูข้อ 3) เก็บไว้เผื่ออนาคต หรือตัดทิ้ง — ต้องถามก่อน
2. **บันทึกโปรแกรมที่ต่อไว้** ตอนนี้ปิดหน้าแล้วหาย และถ้าคัดลอกแล้วไปเลือกตัวอย่างอื่น งานหายโดยไม่เตือน
3. **My Blocks + ตั้งชื่อตัวแปรเอง** ตอนนี้มีแค่ ก/ข/ค ตายตัว (`VARIABLES` ใน blocks/core.ts)
4. **maze ให้มีความจำข้ามเกม** เติม memory API ใน `MazeAgent` + runner/worker แล้วตั้ง `target.memory: true`
   (ลอกแพตเทิร์นจาก othello ได้ทั้งดุ้น)
5. **เพิ่มเกมที่ 3** ลอก pack ของ maze เป็นต้นแบบ — ประกาศ blocks + target.methods + presets + parsers
   เท่านั้น ไม่ต้องแตะ UI

## 7. Open questions for the user

- **โค้ดโหมดโค้ดที่เข้าไม่ถึงจะเก็บหรือตัด** ถามไปสองรอบแล้วยังไม่ได้คำตอบ — อย่าลบเองโดยไม่ถาม
- **จะทำ minimax ให้ต่อด้วยบล็อกได้ไหม** เหลืออันเดียวที่ยังทำไม่ได้จริง ติดตรง `OthelloAgent`
  ไม่มี API ให้จำลองลงหมากบนกระดานสมมติ ถ้าจะทำต้องเติม `simulate(board, move)` ก่อน
  แล้วค่อยคิดเรื่องบล็อกเรียกตัวเองซ้ำ — งานใหญ่กว่าชุดวางแผนเส้นทางมาก
- **GA ผลสั่นมาก** fitness มาจากเกมเดียว จะยอมรับแบบนี้ หรือให้ฝึกหลายเกมต่อหนึ่งการตัดสิน
